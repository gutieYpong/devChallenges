// import { fromJS } from "immutable";

// export const layoutState = fromJS({
//   period: 'day',
//   activeId: 'clock',
// })

export const layoutState = ({
  isPopUp: false,
  searchResult: [],
  // activeId: -1,
})